namespace Resat.Models
{
    // not a great way to do this but we probably won't have too many UI elements...
    public enum TweenType
    {
        InteractionPrompt,
        InteractionPromptCorners,
        NamePanel,
        NamePanelCorners,
        DialoguePanel,
        DialoguePanelCorners
    }
}